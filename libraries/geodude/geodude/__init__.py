from pathlib import Path

geodude_root = Path(__file__).parent.parent
libraries_root = geodude_root.parent
repo_root = libraries_root.parent
outputs_root = repo_root / "outputs"
fonts_root = geodude_root / "geodude/assets/fonts"